import React from 'react';
import '../App.css'

const DataBlock = (props) => {
    return (
      <div id="dataBlock">
        <span>{props.text}</span>
      </div>
    )
  };

export default DataBlock;

